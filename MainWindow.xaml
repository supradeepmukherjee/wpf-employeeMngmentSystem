<Window x:Class="EmployeeWpfClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:EmployeeWpfClient.ViewModels"
        Title="Employee Management System" Height="500" Width="900">
    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
        BorderBrush="#B0B0B0"
        BorderThickness="1"
        CornerRadius="4"
        Padding="8"
        Margin="0,0,12,0"
        Background="#F9F9F9">
            <ContentControl Grid.Column="0" Content="{Binding OpenTabs[0]}"/>
        </Border>

        <Grid Grid.Column="1" Margin="8,0,8,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <ItemsControl Grid.Row="0" ItemsSource="{Binding OpenTabs}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <StaticResource ResourceKey="TabHeaderTemplate"/>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <Border Grid.Row="1"
        Margin="0,8,0,0"
        BorderBrush="#B0B0B0"
        BorderThickness="1"
        CornerRadius="4"
        Padding="8"
        Background="White">
                <ContentPresenter Grid.Row="1" Content="{Binding SelectedTab}" />
            </Border>
        </Grid>
    </Grid>
</Window>